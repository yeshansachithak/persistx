{
  "name": "persistx",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo-vite"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:order": "npm run build -w @persistx/core && npm run build -w @persistx/cli && npm run build -w @persistx/adapter-firestore && npm run build -w @persistx/validator-zod && npm run build -w vite-project",
    "demo": "npm run dev -w vite-project",
    "build": "npm run -ws build",
    "dev": "npm run -ws dev",
    "test": "npm run -ws test",
    "lint": "npm run -ws lint",
    "clean": "npm run -ws clean"
  },
  "devDependencies": {
    "@types/node": "^25.0.3"
  }
}
